import: 
  - recipe/laravel.php

config:
  repository: 'git@github.com:HamiltonMultimedia/Laravel-Blog.git'

hosts:
  laravel-blog.terencehamilton.com:
    remote_user: 'terence_p_hamilton'
    deploy_path: '~/laravel-blog'
  www.laravel-blog.terencehamilton.com:
    remote_user: 'terence_p_hamilton'
    deploy_path: '~/laravel-blog'

tasks:
  build:
    - run: uptime  

after:
  deploy:failed: deploy:unlock
